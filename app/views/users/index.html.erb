<h3>Users</h3>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %> - <%= user.followers(User).count %> followers / following <%= user.followees(User).count %>
    </li>

  <% end %>
</ul>
